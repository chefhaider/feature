# Install PyTorch first — choose the right variant for your system:
#   CPU only:  pip install torch==2.6.0 torchaudio==2.6.0
#   CUDA 12.4: pip install torch==2.6.0+cu124 torchaudio==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
#
# Then install the rest:
#   pip install -r requirements.txt

# Transformers & HuggingFace
transformers==5.8.0
datasets==2.18.0
huggingface_hub==1.14.0
tokenizers==0.22.2

# Audio
torchaudio==2.6.0        # MMS forced aligner (src/align.py) — version must match torch
soundfile==0.13.1
librosa==0.11.0

# Phonology (pure-Python, no system binaries needed)
gruut[de,es]==2.4.0      # text -> IPA
panphon==0.22.2          # IPA -> articulatory features

# ML & numerics
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2

# Plotting & utilities
matplotlib==3.10.9
tqdm==4.67.3
