uid: "004"
date: 2026-05-10
week: W03
stage: extraction
status: failed

model:
  tag: base
  hf_id: facebook/wav2vec2-base
  pretraining: English only
  n_layers_incl_input: 13
  hidden_size: 768
  legacy_tag_in_early_csvs: base
  fine_tuned: false   # models are frozen throughout the project

dataset:
  name: google/fleurs
  split: train
  languages: [en_us, de_de, es_419]
  utterances_per_language: 100
  sampling_rate_hz: 16000

condition:
  summary: first SLURM submission
  frame_to_phoneme: "na"
  pooling: "na"
  transfer_protocol: "na"
  features_probed: []

hyperparameters:
  script: scripts/extract_features.py

seeds:
  master_seed: "na"
  n_independent_seeds_run: 0
  tier_a_three_seed_requirement: not_applicable
  variance_source: |
    Single master seed. sklearn random_state=0 is fixed in probing.py for
    evaluate_probe, cross_lingual_probe and paired_gap_kfold. Spread is
    reported from repeated splits and bootstraps under that one seed, not from
    independent seeds.

compute:
  cluster: FAU HPC (NHR@FAU), $WORK/BIMAP-FEATURE
  partition: v100
  node: tg071
  slurm_job_ids: [1596403]
  wall_clock_min: 0.0
  conda_env: feature

results:
  metric: "na"
  within_language: "na"
  cross_lingual: "na"
  majority_baseline: "na"

artifacts:
  primary: slurm/logs/feature_extraction_1596403.err

provenance:
  confidence: confirmed
  reconstructed_on: 2026-08-06
  evidence:
    - slurm/logs/feature_extraction_1596403.out
    - slurm/logs/feature_extraction_1596403.err

notes: |
  Failed at startup, no embeddings produced. Three faults in one submission:
  'Unable to locate a modulefile for cuda/12.2'; the conda activate path
  pointed at /home/woody/.conda which does not exist; and the script then
  died on ModuleNotFoundError: No module named 'torch'. Fixed by activating
  $HOME/miniconda3 env 'feature' in later scripts.
